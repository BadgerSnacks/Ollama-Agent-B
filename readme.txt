Ollama must be installed on the host pc

If client does not respond you may need to run "ollama serve" from command line.

you can change the AI model by changing the name in ollama_testing.py, currently it is set to model_name = "mistral". model options can be found on www.ollama.com/search